package pl.krasnowski.greeks.services;

public class PlayerGlobalMethodsImpl implements PlayerGlobalMethods {
	
	void performExperiment() {		
		
		/* metoda klikniecia w przycisk znajdujacy sie w oknie podgladu badań ( bedzie na liscie miast, ilosci kryształów, 
		 * ilosci naukowcow i produkujacych pkty badan
		 * zaznaczajaca innym kolorem, ktore miasto posiada optyka i na ktorym lvl(jesli nie jest to max), 
		 */
	}
	
	// zarzadzanie statkami handlowymi
	// zarzadzanie wojskiem
	// zarzadzanie badaniami
	// interfejs komunikacji z uzytkownikami + chat dla korzystajacych z uslugi serwera

	
//	public void lvlUP() {  // move this 2 to @Service
//	setLvl(getLvl() + 1);
//	//TODO: add ranking points
//}

//public void lvlDown(List<ABuilding> buildings) {
//	setLvl(getLvl() - 1);
//	if (getLvl() < 1)
//	{
//		for (int i = 0; i < buildings.size() - 1; i++)
//		{
//			if (buildings.get(i).getLvl() == 0)
//				buildings.remove(i);
//		}
//	}
//}
}
